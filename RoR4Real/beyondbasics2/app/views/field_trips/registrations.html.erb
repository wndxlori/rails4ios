<h1>Listing Students for <%= @field_trip.name %></h1>
<%= link_to "Export", registrations_field_trip_path(@field_trip, :format => :csv) %>
<ol>
<% @students.each do |student| %>
	<li><%= student.name %></li>
<% end %>
</ol>